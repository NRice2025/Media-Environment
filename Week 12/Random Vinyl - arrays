var size = [];

function setup() {
  createCanvas(600, 600);
  
  for (var i=0; i<100; i++)
    {
      size.push(random(5,500))
    }
}

function draw() {
  background(220);
  
  noFill();
  for (var i=0; i<size.length; i++)
    {
      circle(width/2, height/2, size[i]);
    }
}