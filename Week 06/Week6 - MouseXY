function setup() {
  createCanvas(800, 500);
  background(1, 186, 240);
}

function draw() {
  //declare variables
  var x = mouseX;
  var y = mouseY;
  var size = 50;
  
  //circle
  fill(237, 34, 93, 100);
  noStroke();
  ellipse(x, y, size, size);
}