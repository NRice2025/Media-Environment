var toggle = true;

function setup() {
  createCanvas(300, 800);
}

function draw() {
  if (mouseIsPressed === true)
    {
      toggle = !toggle;
    }
  
  if (toggle === true)
    {
      background (1, 186, 240);
    }
  else
    {
      background (250, 150, 150)
    }
}